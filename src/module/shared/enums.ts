
export enum InjectableScope {
  Transient = 'transient',
  Singleton = 'singleton',
  Request = 'request',
}

export enum ProviderType {
  Class = 'class',
  Value = 'value',
  Factory = 'factory',
}
